sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: 19daf29b7fad9baef7afb30842f3ab1278ddacf1
  keep-history: true
  on:
    branch: master
  local-dir: public