sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: d6ac51fa9e0024cf2693d09c60d5e05df99d37d5
  keep-history: true
  on:
    branch: master
  local-dir: public